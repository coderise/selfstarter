<% if Settings.use_payment_options %>
  <fieldset>
    <div class="payment_options">
      <ol>
        <% PaymentOption.all.each_with_index do |p, index| %>
          <li>
            <%= radio_button_tag "payment_option", p.id, false, class: 'payment_radio' :onchange => 'option.value = payment_option.value' %>
            <%= label_tag("payment_option_#{index}", p.amount_display) %>
            <div class="description">
              <p class="full"><%= raw p.description %></p>
            </div>
          </li>
        <% end %>
      </ol>
    </div>
  </fieldset>
<% end %>